///
/// HybridSharedZustandStoreSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <string>
#include <optional>

namespace margelo::nitro::threadedzustand {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SharedZustandStore`
   * Inherit this class to create instances of `HybridSharedZustandStoreSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSharedZustandStore: public HybridSharedZustandStoreSpec {
   * public:
   *   HybridSharedZustandStore(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSharedZustandStoreSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSharedZustandStoreSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSharedZustandStoreSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::optional<std::string> getState(const std::string& storeName, const std::string& subtreeKey) = 0;
      virtual std::string getOrInitState(const std::string& storeName, const std::string& subtreeKey, const std::string& initialJson, const std::string& persistKey) = 0;
      virtual double setState(const std::string& storeName, const std::string& subtreeKey, const std::string& stateJson) = 0;
      virtual double getRevision(const std::string& storeName, const std::string& subtreeKey) = 0;
      virtual double clear(const std::string& storeName, const std::string& subtreeKey) = 0;
      virtual void setPersistedState(const std::string& persistKey, const std::string& stateJson) = 0;
      virtual void clearPersistedState(const std::string& persistKey) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SharedZustandStore";
  };

} // namespace margelo::nitro::threadedzustand
